# start ssh-agent if it's not already up
if [ -z "$SSH_AUTH_SOCK" ] ; then
    eval `ssh-agent`
fi

# add
ls ~/.ssh/id_* | grep -v ".pub" | xargs ssh-add

# add connect to path
PATH=~/.ssh/connect:$PATH
