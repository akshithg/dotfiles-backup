
# define update function
function update() {
  echo "Updating apt..."
  sudo apt update
  echo "Upgrading apt..."
  sudo apt upgrade
  echo "Cleaning up apt..."
  sudo apt autoremove
  echo "Done!"
}
